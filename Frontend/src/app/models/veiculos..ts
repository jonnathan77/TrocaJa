export interface Brand {
  name: string;
  logo: string;
  codigo: string; // opcional se nem sempre tiver
}

export interface Model {
  nome: string;
  codigo: string;
}

export interface Ano {
  nome: string;
  codigo: string;
}
